{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Executar Bot Mercado Livre",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "envFile": "${workspaceFolder}/.env",
            "showLog": true
        }
    ]
}